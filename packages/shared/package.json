{
  "name": "@shortener/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared code for URL Shortener (types, validators, constants, utils)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "watch": "tsc --watch"
  },
  "keywords": [
    "serverless",
    "aws-lambda",
    "url-shortener"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
