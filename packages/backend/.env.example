# ==============================================
# Backend Environment Variables
# ==============================================
# Copy this file to .env and fill with your values
# NEVER commit .env to git!

# JWT Secret (REQUIRED)
# Generate a secure secret: openssl rand -base64 32
# This key is used to sign and verify JWT tokens
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# DynamoDB Table Name (Optional - auto-generated by Serverless)
# Format: url-shortener-{stage}
# TABLE=url-shortener-local

# Deployment Stage (Optional - defaults to 'local')
# Options: local, dev, prod
# STAGE=local

# AWS Region (Optional - configured in serverless.ts)
# AWS_REGION=sa-east-1

# ==============================================
# Production Notes
# ==============================================
# In production (AWS Lambda), configure these variables as:
# 1. Lambda Environment Variables (for JWT_SECRET)
# 2. AWS Secrets Manager (recommended for sensitive data)
# 3. AWS Systems Manager Parameter Store

# Security Best Practices:
# - Use different JWT_SECRET for each environment
# - Rotate secrets periodically
# - Use AWS Secrets Manager in production
# - Enable encryption at rest for sensitive data
