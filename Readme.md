# 1. Clonar o repositório
git clone <seu-repo>
cd url-shortener

# 2. Abrir no VS Code
code .

# 3. Quando aparecer a notificação:
#    "Reopen in Container?" → Clicar em [Reopen in Container]

# 4. Aguardar (primeira vez demora ~2min):
#    - Container buildar (Alpine + Node LTS)
#    - npm install rodar automaticamente
#    - Extensões VS Code instalarem